# =============================================================================
# Environment Variables for pedrocastro.pro
# =============================================================================
# Copy this file to .env.local for development
# For Docker: pass these via docker-compose.yml or -e flags
# =============================================================================

# Gmail SMTP Configuration (required for contact form)
# Generate app password at: Google Account > Security > 2-Step Verification > App Passwords
GMAIL_USER=your-gmail@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password
GMAIL_RECIPIENT=recipient@example.com
